services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    working_dir: /app
    env_file: .env
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    environment:
      - HOME=/tmp
    volumes:
      - ./src:/app/src
      - ./data:/data
      - ./snapshots:/snapshots
      - ./reports:/reports
      - ./logs:/logs
    shm_size: "2gb"
    command: ["bash", "-lc", "python -m src.main"]

  dashboard:
    build:
      context: .
      dockerfile: docker/Dockerfile
    working_dir: /app
    env_file: .env
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    environment:
      - HOME=/tmp
    volumes:
      - ./src:/app/src
      - ./data:/data
      - ./reports:/reports
      - ./logs:/logs
    shm_size: "2gb"
    ports:
      - "127.0.0.1:8501:8501"
    command: ["bash", "-lc", "streamlit run src/dashboard.py --server.address 0.0.0.0 --server.port 8501"]
